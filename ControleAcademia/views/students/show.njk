{% extends "layout.njk" %}

{% block header %}

<div class="links-desafios">
  <a href="/teachers" class="active">Teachers</a>
  <a href="/students">Students</a>
</div>
{% endblock header %}

{%block content%}
  <div class="card-teacher">
  <section class="avatar-teacher" style="background: url({{student.avatar_url}}) no-repeat center center / cover">

  </section>

  <section class="details-teacher">

    <h3>Detalhes</h3>

    <div class="itens">
      <div>Professor</div>
      <div>{{student.name}}</div>
    </div>

    <div class="itens">
      <div>Idade</div>
      <div>{{student.birth}}</div>
    </div>

    <div class="itens">
      <div>Grau de Escolaridade</div>
      <div>{{student.grade}}</div>
    </div>

    <div class="itens">
    <div>Acompanhamento</div>
      <div>
        {% for lession in student.lessioning %}
        <span>{{lession}}</span>
        {% endfor %}
      </div>
    </div>

    <div class="itens">
      <div>Desde</div>
      <div>{{student.created_at}}</div>
    </div>

    <a href="/desafios/{{student.id}}/edit" class="button">Editar</a>

  </section>


  </div>
{%endblock content%}